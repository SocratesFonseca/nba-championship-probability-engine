# Runtime Artifacts

This folder contains only the small, non-secret files required to serve the
existing model on Railway: the trained pipeline, its metadata, and validated
team-season feature rows used for historical predictions.

Raw NBA API responses, processed training outputs, databases, and credentials
remain ignored.
